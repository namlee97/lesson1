//Viết một hàm JavaScript để kiểm tra xem một chuỗi rỗng không.
let input = "fyggryfgry39455" ;
input = null ;
let isResult = true ;
if (input == "") {
    isResult = true
} else {
    isResult = false
};
console.log(isResult);